<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.MemberDAO">

    <!-- 특정 청원 조회 -->
    <select id="selectMember" parameterType="memberVO" resultType="memberVO">
        SELECT user_id as userId, email, password, name, role, status, created_at as createdAt, updated_at as updatedAt
          FROM TB_USER
         WHERE USER_ID = #{userId, jdbcType=VARCHAR}
           AND PASSWORD = #{password, jdbcType=VARCHAR}
    </select>

	<insert id="insertMember" parameterType="memberVO">
        INSERT INTO TB_USER (USER_ID, EMAIL, PASSWORD, NAME, ROLE, STATUS, CREATED_AT, UPDATED_AT)
		VALUES (#{userId}, #{email}, #{password}, #{name}, 'USER', 'ACTIVE', SYSDATE, NULL)
    </insert>

</mapper>

